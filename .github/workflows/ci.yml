name: CI

# Trigger the workflow on push or pull request, but only for the master branch
on:
  pull_request:
  push:
    branches: [main]

jobs:
  build:
    name: Building on ${{ matrix.os }} with ghc-${{ matrix.ghc }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            cabal: "3.4.0.0"
            ghc: "8.10.7"
          - os: macos-latest
            cabal: "3.4.0.0"
            ghc: "8.10.7"
    steps:
    - uses: actions/checkout@v2
    - uses: haskell/actions/setup@v1
      name: Setup Haskell
      with:
        ghc-version: ${{ matrix.ghc }}
        cabal-version: ${{ matrix.cabal }}
    - uses: actions/cache@v1
      name: Cache ~/.cabal/store
      with:
        path: ~/.cabal/store
        key: ${{ runner.os }}-${{ matrix.ghc }}-cabal
    - name: Install system dependencies
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends ninja-build libhdf5-dev
    - name: Install system dependencies
      if: matrix.os == 'macos-latest'
      run: |
        brew install libomp hdf5 
    - name: Build lattice-symmetries
      run: |
        mkdir -p third_party
        cd third_party
        git clone --recursive https://github.com/twesterhout/lattice-symmetries.git
        cd lattice-symmetries
        cmake -Bbuild \
              -DCMAKE_BUILD_TYPE=Release \
              -DCMAKE_INSTALL_PREFIX=$GITHUB_WORKSPACE/lattice-symmetries \
              -DLatticeSymmetries_ENABLE_UNIT_TESTING=OFF
        cmake --build build -j4
        cmake --build build --target install
        cd ..
    - name: Start SSH session
      if: matrix.os == 'macos-latest'
      uses: luchihoratiu/debug-via-ssh@main
      with:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}
        SSH_PASS: ${{ secrets.SSH_PASS }}
    - name: Build
      run: |
        export PKG_CONFIG_PATH=$GITHUB_WORKSPACE/lattice-symmetries/lib/pkgconfig
        cd c
        make
        cd ..
        export LD_LIBRARY_PATH=$PWD/c/build:$LD_LIBRARY_PATH
        export DYLD_LIBRARY_PATH=$PWD/c/build:$DYLD_LIBRARY_PATH
        cc -Icbits `pkg-config --cflags lattice_symmetries` main.c `pkg-config --libs lattice_symmetries` -L$PWD/c/build -llattice_symmetries_core
        ./a.out

        cabal v2-update
        cabal v2-build --disable-tests --extra-lib-dirs=$PWD/c/build || true
        cat /Users/runner/work/lattice-symmetries-haskell/lattice-symmetries-haskell/dist-newstyle/build/x86_64-osx/ghc-8.10.7/lattice-symmetries-haskell-0.1.0.0/build/LatticeSymmetries/FFI_hsc_make
        # - name: Test
        #   run: |
        #     cabal v2-test --enable-tests --test-show-details=direct
